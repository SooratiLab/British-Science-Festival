# Dependencies for narrator.py — install these on the Raspberry Pi 5,
# not on the Jetson (narrator.py runs standalone on the Pi 5 and connects
# to the Jetson's rosbridge over Tailscale).
#
#   pip install -r requirements.txt
#   sudo apt install espeak

websocket-client>=1.6